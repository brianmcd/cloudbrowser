#!/bin/bash
if [ "$npm_package_name" == "jsdom" ]; then
	echo Applying CloudBrowser patches to JSDOM
    if [ -f "../../jsdom_patch/jsdom.diff" ]; then
        patch --verbose -p0 < ../../jsdom_patch/jsdom.diff
        patch --verbose -p0 < ../../jsdom_patch/lenthFromProperties.diff
    fi
fi
